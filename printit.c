#include <stdio.h>

int main()
{
    long long int a;
    long long int b;
    long long int result;
    scanf("%lld", &a);
    scanf("%lld", &b);
    result = a * b;
    printf("%lld", result);

    return 0;
}